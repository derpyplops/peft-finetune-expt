# vendored upstream repos (cloned locally for reference; not ours to track)
experiments/*/vendor/

# python / os cruft
__pycache__/
*.pyc
.DS_Store

# run artifacts (regenerated on the GPU box)
saves/
saves_multiple/
*.ckpt
wandb/
